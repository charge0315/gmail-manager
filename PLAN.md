# ZenMail Automator: 徹底整理プロジェクト計画書

## 概要
AI（Gemini 3.1 Pro）を活用し、過去1年間の全メールデータを分析した上で、最適な15のカテゴリによるGmailの自動整理・再構築を行います。
また、本機能を MCP (Model Context Protocol) サーバーとして提供し、AI アシスタントからの操作を可能にします。

## 実行フェーズ

### 1. 基盤の完全初期化 (Reset Phase) ✅
- **目的**: 既存のノイズを排除し、クリーンな状態で再構築を開始する。
- **タスク**:
  - [x] 既存の全ユーザー定義ラベルの削除。
  - [x] 既存の全自動振り分けフィルタの削除。
  - [x] 全メール（アーカイブ済みを含む）からラベルを剥がし、受信トレイ（INBOX）へ集約。

### 2. 全量データ抽出 (Data Fetching Phase) ✅
- **目的**: 過去1年間の全てのメールパターンを把握する。
- **タスク**:
  - [x] 検索クエリ `newer_than:1y` を使用。
  - [x] ページネーションを用いて、1年分の全メールの「送信元」と「件名」をループ取得。

### 3. Gemini 3.1 Pro による高度分析 (Analysis Phase) ✅
- **目的**: 15個の最適な分類ルールとクエリを生成する。
- **タスク**:
  - [x] 使用モデル: `gemini-3.1-pro-preview` / `gemini-3.1-flash`
  - [x] 分類数: 最大15個 + その他。
  - [x] カラー設定: Gmail API標準パレット。

### 4. 適用と自動化 (Execution Phase) ✅
- **タスク**:
  - [x] ラベル作成（正しい色付き）。
  - [x] 全メールに対し、作成したルールを一括適用。
  - [x] 今後の受信メールに対し、自動振り分けフィルタを有効化。

### 5. MCP サーバー統合と Docker 化 (Integration Phase) ✅
- **目的**: Claude 等の AI アシスタントから利用可能にする。
- **タスク**:
  - [x] FastMCP によるサーバー実装 (`server.py`)。
  - [x] Dockerfile の作成と非特権ユーザーでのセキュリティ強化。
  - [x] コンテナ内での環境変数管理。

## 今後の展望
- [ ] マルチアカウント対応。
- [ ] メールの本文も含めたより高度な分析（現在は件名と送信者のみ）。
- [ ] 定期的な自動実行ジョブのコンテナ内への統合。

## 技術的対策
- **IPv4強制**: Windows環境のIPv6接続不良によるタイムアウトを回避。
- **待機インターバル**: APIレート制限を回避するため、リクエスト間に1秒の待機を挿入。
- **リトライ強化**: 一時的なネットワークエラー時に最大5回まで自動再試行。
